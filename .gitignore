# Dependency directories
/node_modules
/dist
/build

# OS generated files
.DS_Store
Thumbs.db

# Editor specific
.vscode/
.idea/

# Logs and temporary files
*.log
*.tmp
*.swp

# Artifacts for HTML/Static Sites (Since the original repo was HTML/Static)
/coverage

# Build artifacts (If any compilation steps were added later, though this is static)
/out

# Configuration and secrets (Safety first)
.env
.env.*
!*.example.env

# CI/CD artifacts (GitHub Actions temporary storage)
.github/workflows/*.lock